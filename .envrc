# module load gcc cuda/12.2
# conda activate r3dg 

conda activate gs_8.6
module load gcc cuda/12.2

source .cmdrc


shopt -s histappend
PROMPT_COMMAND="history -a;$PROMPT_COMMAND"
HISTSIZE=1000
HISTFILESIZE=2000
HISTFILE=.bash_history
history -r